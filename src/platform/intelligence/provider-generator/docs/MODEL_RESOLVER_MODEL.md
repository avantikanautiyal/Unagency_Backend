# Model Resolver Model

`resolveModel(DesiredCapabilityProfile, inventory)` scores discovered models on:

capability · modality · context window · reasoning · streaming · tools ·
structured outputs · vision · audio · embeddings · budget preference · region

Business modules never see required vendor model brand names as inputs.
