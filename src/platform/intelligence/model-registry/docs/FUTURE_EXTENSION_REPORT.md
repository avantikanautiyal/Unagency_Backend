# M5.1 Model Registry — Future Extension Report

## External inventory sync

Load manifests from external Excel/JSON without architectural changes — seed loader pattern extends.

## Adapter bridge

Frozen adapter `ProviderManifest` may be projected FROM canonical manifests (ACP if needed).

## Regional pricing

Per-region pricing tiers on `ModelPricing`.

## What stays frozen

All M5.1 contracts and frozen M1–M4 modules.
