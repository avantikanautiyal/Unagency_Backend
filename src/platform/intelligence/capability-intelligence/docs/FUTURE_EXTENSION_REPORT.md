# Future Extension Report — Capability Intelligence

## Near term

1. Sync taxonomy with `capability-registry` manifests.
2. Experience-weighted scoring using `ExecutionExperiencePackage`.
3. Persist evolution metrics per capability version.

## Medium term

1. Wire `IExecutionIntelligenceCapabilityConsumer` once EI unfreezes ports.
2. Industry packs (healthcare, fintech) extending taxonomy.
3. Automatic bundle marketplace / reuse library.

## Long term

OS thinks entirely in capabilities. Providers and models remain interchangeable
implementations behind capability contracts.

## Deferred

- Live provider probing
- Automatic routing from capability plans
- Prompt generation from capabilities
