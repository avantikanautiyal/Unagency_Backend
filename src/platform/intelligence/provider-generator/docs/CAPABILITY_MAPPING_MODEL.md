# Capability Mapping Model

Manifest `capabilityMatrix` maps modalities/features → canonical capability IDs:

- `marketing.copywriting`
- `software.code_generation`
- `design.image_generation`
- …

Generated `mapModelToCapabilities()` binds discovered models to these IDs.
