# Activity Model

`StudioActivity` kinds: user_action, ai_action, review, approval, comment,
version, artifact, execution, assignment, presence.

`StudioTimeline` orders activity ids per workspace.

`StudioTask` tracks assignments used by task widgets and studios.
