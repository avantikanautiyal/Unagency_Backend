/**
 * Adapter emission notes — generated adapters follow OpenAI leaf pattern.
 */

export const ADAPTER_INTEGRATION_NOTES = [
  "Implement translateRequest / normalizeResponse",
  "Resolve model via ModelResolver from discovered inventory",
  "Register with Adapter Platform registry without modifying adapters framework",
] as const;
