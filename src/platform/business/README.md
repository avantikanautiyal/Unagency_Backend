# Business Platform

SaaS product layer above direct execution. Delegates AI work exclusively through the Enterprise API Gateway.

See `src/platform/business/engine/` and `api/docs/MASTER_API_REFERENCE.md`.
