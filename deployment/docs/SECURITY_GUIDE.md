# Security Guide

See `deployment/security/README.md`.

TLS termination at Nginx/Ingress, security headers, container hardening, dependency + image config scanning in CI, certificate placement under `deployment/certificates/`.
