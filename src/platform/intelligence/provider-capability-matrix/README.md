# Provider Capability Matrix (compatibility shim)

**Moved in M1.3** to `src/platform/intelligence/providers/capability-matrix/`.

This folder re-exports the matrix for backward compatibility.

Use:

```typescript
import { ProviderCapabilityMatrix } from "../providers";
```
