# Merge Model

Modes: `none`, `paragraphs`, `json`, `structured`, `citations`, `summaries`,
`code`, `reasoning`.

Merges always record `sources` / role attribution. Blind merges are forbidden.

## Location

`aggregation/merge-engine.ts`
