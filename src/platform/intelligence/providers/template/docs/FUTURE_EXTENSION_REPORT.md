# M5.1.1 Provider Template — Future Extension Report

## Transport layer hook

Frozen transport platform wires in at SDK `dispatch()` — template defines contract boundary only.

## Identity integration

Authentication placeholder replaced with Provider Identity contracts when implementing real providers.

## Copy script

CLI `copy-provider-template` to scaffold new provider folders from skeleton.

## What stays frozen

M5.1.1 template contracts/interfaces and all M1–M5.1 modules.
