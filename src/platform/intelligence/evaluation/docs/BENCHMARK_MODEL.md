# Benchmark Model

`BenchmarkProfile` scoped by capability, industry, department, complexity,
risk, provider, model, version.

Target score derived from risk baseline and historical EvaluationReport means.
Adaptive passing score via `adaptPassingScore`.
